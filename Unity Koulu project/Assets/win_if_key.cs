using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class win_if_key : MonoBehaviour
{
    // Start is called before the first frame update

    public bool winned = false;
  
    void Start()
    {
         
    }

    // Update is called once per frame
    void Update()
    {

        
    }
}
